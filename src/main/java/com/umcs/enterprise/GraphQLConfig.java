package com.umcs.enterprise;

import org.springframework.context.annotation.Configuration;

@Configuration
public class GraphQLConfig {
//    public GraphQLConfig(BatchLoaderRegistry registry, BookRepository repository) {
//
//        registry.forTypePair(Long.class, Book.class).registerMappedBatchLoader((ids, env) -> {
//            return Mono.just(repository.findAllById(ids).stream().collect(Collectors.toMap(Book::getDatabaseId, Function.identity())));
//        });
//
//        // more registrations ...
//    }


}
