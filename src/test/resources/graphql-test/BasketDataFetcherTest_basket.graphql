query BasketDataFetcherTestBasket($id: ID) {
	basket(id: $id) {
		id
		books(first: 10) {
			edges {
				node {
					id
					title
				}
				price {
					formatted
					raw
				}
				quantity
			}
		}
		price {
			formatted
			raw
		}
	}
}
