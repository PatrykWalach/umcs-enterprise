query BookControllerTest_books($after: String, $before: String, $first: Int, $last: Int) {
	books(first: $first, last: $last, before: $before, after: $after) {
		edges {
			node {
				id
			}
		}
		pageInfo {
			hasNextPage
			hasPreviousPage
			startCursor
			endCursor
		}
	}
}
